# GPT-5.6 API 检测报告

## 结论

本次未发现 `Sub2API` 将申报的 `gpt-5.6-sol` 混用为 Terra 或 Luna 的证据。

- Juice：通过。15/15 个有效样本均匹配申报模型，混用 0，失败 0。
- 行为指纹：Sol 相对匹配度 91.80%，Luna 6.89%，Terra 1.30%；参考赢家为 Sol。
- 输出完整性：32/48 两项均精确返回，2/2 通过，无 40/40xxx 改写。
- 显式 Juice 覆盖：2/2 返回显式值，未发现隐藏覆盖。
- 网络：49/49 成功，HTTP 200；0 重试、0 失败、0 取消。

综合判断：本次样本支持“请求实际表现与 GPT-5.6 Sol 一致”。但检测只能证明本批固定探针未达到报警门槛，不能排除透明代理、探针识别、时段性路由或官方风控导致的临时变化。

## 测试配置

| 项目 | 值 |
|---|---|
| 时间 | 2026-08-31 12:16:06 - 12:17:34（Asia/Shanghai） |
| 检测器 | `gpt56_api_detector` v4.1.1 Release |
| Release 校验 | SHA-256 清单内 35 个文件全部通过 |
| CC Switch 当前供应商 | `Sub2API` |
| 申报/请求模型 | `gpt-5.6-sol` |
| 协议 | Responses，原生 Codex 请求形状 |
| 基础档位 | 中档样本量，49 个逻辑请求 |
| 并发 | 3 |
| 重试上限 | 每个任务 2 次；实际重试 0 次 |
| 历史上下文 | 无历史 |
| 原始请求/响应留存 | 关闭 |

供应商限制普通 Responses 客户端，普通模式会返回 HTTP 403：`This account only allows Codex official clients`。因此正式测试按检测器自带的完整 `native_codex` 请求格式执行。

修改并发数和请求格式后，检测器按规则把本次标记为“自定义参考”。因此 91.80% 是相对匹配度，不是路由概率，也不构成官方档位的正式“强烈指向”判定。

## 探针结果

| 探针 | 样本 | 结果 |
|---|---:|---|
| Juice low | 3/3 | 全部为申报模型成功 |
| Juice high | 6/6 | 全部为申报模型成功 |
| Juice xhigh | 3/3 | 全部为申报模型成功 |
| Juice max | 3/3 | 全部为申报模型成功 |
| 32/48 输出完整性 | 2/2 | 精确返回，无硬异常 |
| 显式 Juice 覆盖 | 2/2 | 显式值，无硬异常 |
| 随机国家 | 10/10 | 完成，Sol 指纹贡献占优 |
| 随机鸟 | 10/10 | 全部 `kingfisher` |
| b80 字符计数 | 10/10 | 全部 `exact_3` |

行为指纹三个窗口均 100% 完成，最低正式完成要求为 9/10；没有无效输出、网络错误或取消。

## 延迟

### 49 个检测请求总耗时

这里是从建连到 `response.completed` 的单请求耗时，不是首 token：

| 指标 | 耗时 |
|---|---:|
| 最快 | 3.648 s |
| 中位数 | 5.117 s |
| 平均 | 5.279 s |
| P95 | 7.672 s |
| 最慢 | 12.305 s |

### 首 token 基准

另用相同原生 Codex 请求形状执行 12 个 `low` 短请求，并发 3；12/12 均为 HTTP 200。

“首 token”定义为从开始 TCP/TLS 建连到收到第一个 `response.output_text.delta` 或可见 reasoning delta。另列首 SSE 事件，避免把 `response.created` 当作模型 token。

| 指标 | 首 SSE 事件 | 首个可见输出 token | 完整响应 |
|---|---:|---:|---:|
| 最快 | 2.299 s | 2.911 s | 2.931 s |
| 中位数 | 3.339 s | 4.392 s | 4.435 s |
| 平均 | 3.822 s | 4.819 s | 4.843 s |
| P95 | 4.735 s | 5.726 s | 5.880 s |
| 最慢 | 8.233 s | 9.434 s | 9.434 s |

样本数为 12，P95 只适合作为本次短时观察，不应视为长期 SLA。

## 边界与建议

1. 结论仅代表 2026-08-31 本次 49 个检测请求和 12 个延迟请求。
2. Juice 是辅助指纹，理论上可伪造；相对匹配度不是“有多少请求被路由到某模型”。
3. 当前账号只允许官方客户端请求形状，无法用检测器的普通 Responses 官方中档原样执行。
4. 若用于采购或稳定性验收，建议在不同时段重复 3 轮，并分别记录并发 1 和并发 3 的首 token 分布。

## 原始材料

- 检测器原始 JSON：`gpt56-api-detector-report-2026-08-31-raw.json`
- 首 token 明细：`gpt56-api-detector-report-2026-08-31-ttft.json`
- 检测 Session：`d93f9a9730344a37aa9b6eb433fba900`
- 配置哈希：`1a16ee82aeabdf87fffbcc27dcdd42b4960fb798f8d564e926bfa24fd5e8cbf0`

报告未保存 API Key、Authorization 或 Key 哈希。
